<%= form_for [:admin, @trip] do |f| %>
    <%= f.label :duration %>
    <%= f.number_field :duration %>
    <%= f.label :bike_id %>
    <%= f.number_field :bike_id %>
    <%= f.label :start_date %>
    <%= f.date_field :start_date %>

<div class="form-group row">
  <label class="col-sm-2 col-form-label">
    <%= f.label :start_station_id%></label>
    <div class="col-sm-10">
    <%= f.collection_select(:start_station_id, Station.order(:name), :id, :name) %>
  </div>
</div>

    <%= f.label :end_date %>
    <%= f.date_field :end_date %>
  <div class="form-group row">
    <label class="col-sm-2 col-form-label">
    <%= f.label :end_station_id %> </label>
    <div class="col-sm-10">
      <%= f.collection_select(:end_station_id, Station.order(:name), :id, :name)%></label>
      </div>
    </div>

    <div class="form-group row">
      <label class="col-sm-2 col-form-label">
        <%= f.label :subscription_type %> </label>
        <div class="col-sm-10">
    <%= f.collection_select(:subscription_type, Station.order(:subscription_type), :id, :subscription_type)%>      
    </div>
    </div>

    <%= f.label :zip_code %>
    <%= f.number_field :zip_code %>
    <%= f.submit %>
<% end %>
